node_modules
PRD
.next/
# All env files — never commit secrets (allow example templates)
.env
.env.*
!.env.example

# Local Claude Code / preview config (machine-specific)
.claude/

# TS incremental build cache
tsconfig.tsbuildinfo
